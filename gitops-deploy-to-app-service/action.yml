name: 'Deploy to Azure App Service'
description: 'Deploy the application to Azure App Service'
inputs:
  azure_credentials:
    description: 'Azure Credentials, stored in a secret on your GitHub repository, generated by https://github.com/microsoft/NubesGen/blob/main/docs/gitops-quick-start.md'
    required: true
runs:
  using: "composite"
  steps:
    - name: Download application name
      uses: actions/download-artifact@v2
      with:
        name: application_name
    - name: Set environment variables
      run: |
        echo "APPLICATION_NAME=$(cat application_name.txt)" >> $GITHUB_ENV
      shell: bash
    - name: Download application package
      uses: actions/download-artifact@v2
      with:
        name: package
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{ inputs.azure_credentials }}
    - name: 'Deploy to Azure App Service'
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.APPLICATION_NAME }}
        package: '*'
